spring:
  profiles:
    active: test

---

spring:
  profiles: dev
  application:
    name: microservicecloud-config-nocol-dev

---

spring:
  profiles: test
  application:
    name: microservicecloud-config-nocol-test       